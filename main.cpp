#include <iostream>
#include <fstream>
#include <cstdlib>
#include <string>

#include "inFile.h"

using namespace std;


main(){
    //file and take in
    inFile File;
    string s = "sillyFile.txt";
    File.attachFile(s);


    cout << "Start scanning"
    tok = scan();
    
    File.closeFile();
    return 0;
}